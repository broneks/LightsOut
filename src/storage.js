define(function() {
  'use strict';

  // function save(obj) {
  //  localStorage.setItem( obj, JSON.stringify(obj) );
  // }  

  // function load(id) {
  //  return JSON.parse( localStorage.getItem(id) );
  // }
});